---
name: scaffolding-templating
description: "Turn best practices into ready-to-use templates, scaffolds, token systems, and reference databases so output quality stays consistent instead of being reinvented from scratch each time. Covers general content/page scaffolding (programmatic SEO page templates) and a full design sub-system: brand identity, design tokens, UI styling (shadcn/Tailwind), logo/banner/slide/theme/icon generation, algorithmic art, canvas posters, Anthropic brand guidelines, web-artifact building, UI/writing-style compliance review, a cluster of anti-AI-slop frontend taste systems (structured brief-to-design-system pipelines, macrostructure/theme catalogs, brutalist/minimalist/high-end-agency aesthetic specs, image-first and image-only design-direction workflows, existing-site redesign audits), and a deep animation/motion-engineering system (building, reviewing, auditing, and finding opportunities for interface motion; a reverse-lookup animation-terminology glossary; Apple's fluid-interface/spring-physics principles; curated UI-library picks; multi-variant UI prototyping behind a visual picker). Use whenever the user wants to scaffold a new project/page/deliverable from a template or data-backed pattern, needs consistent visual design (colors, typography, layout, components, presentations, brand assets, themes), wants a UI or docs review against a design/writing style guide, wants a landing page or portfolio that doesn't look like generic AI output, wants an interface animation built/reviewed/audited or wants to know what an animation effect is called, needs a UI library recommendation, wants several UI variants to compare live, or says things like 'make this look good', 'set up the design system', 'generate a banner/logo/slide deck', 'build pages at scale from this data', or 'make this feel more alive'."
---

# Scaffolding & Templating

Router skill for "codify the best-practice pattern once, then stamp it out consistently" — as opposed to solving the same design/structure problem freshly every time. Two sub-groups: general scaffolding, and a much larger design & visual templating cluster (`references/design/`).

## General scaffolding

| Domain | When to use | Full guide |
|---|---|---|
| Programmatic SEO pages | Generate SEO-driven pages at scale from templates + data (directory pages, location pages, "[keyword] + [modifier]" patterns) | [references/general/programmatic-seo/SKILL.md](references/general/programmatic-seo/SKILL.md) |

## Design & visual templating

| Domain | When to use | Full guide |
|---|---|---|
| Design (umbrella) | Brand identity, design tokens, logo generation, CIP mockups, HTML presentations, banners, icons, social photos — the broadest entry point | [references/design/design/SKILL.md](references/design/design/SKILL.md) |
| Design system | Three-layer design tokens (primitive→semantic→component), CSS variables, spacing/typography scales, component specs | [references/design/design-system/SKILL.md](references/design/design-system/SKILL.md) |
| UI styling | shadcn/ui + Radix + Tailwind components, canvas-based visual designs, dark mode, accessible patterns | [references/design/ui-styling/SKILL.md](references/design/ui-styling/SKILL.md) |
| UI/UX design intelligence | Searchable database of styles, palettes, font pairings, charts, and per-stack conventions (React, Next.js, Vue, Flutter, etc.) | [references/design/ui-ux-pro-max/SKILL.md](references/design/ui-ux-pro-max/SKILL.md) |
| Banner design | Social/ad/web-hero/print banners across platforms with art-direction options | [references/design/banner-design/SKILL.md](references/design/banner-design/SKILL.md) |
| Brand voice & identity (build your own) | Define/apply a *new* brand's tone of voice, messaging framework, visual identity, asset management, consistency rules | [references/design/brand/SKILL.md](references/design/brand/SKILL.md) |
| Anthropic's own brand guidelines (apply preset) | Apply Anthropic's specific, pre-existing colors/typography to an artifact — not for building a generic brand system | [references/design/anthropic-brand-guidelines/SKILL.md](references/design/anthropic-brand-guidelines/SKILL.md) |
| Canvas design | Beautiful static visual art as `.png`/`.pdf` — posters, original artwork | [references/design/canvas-design/SKILL.md](references/design/canvas-design/SKILL.md) |
| Algorithmic art | Generative art with p5.js, seeded randomness, flow fields, particle systems | [references/design/algorithmic-art/SKILL.md](references/design/algorithmic-art/SKILL.md) |
| Theme factory | Apply one of 10 preset color/font themes to any artifact (slides, docs, HTML pages) | [references/design/theme-factory/SKILL.md](references/design/theme-factory/SKILL.md) |
| Frontend design direction | Aesthetic direction, typography, and intentional choices that avoid templated-default look | [references/design/frontend-design/SKILL.md](references/design/frontend-design/SKILL.md) |
| Web artifacts builder | Elaborate multi-component claude.ai HTML artifacts — React/Tailwind/shadcn, state management, routing | [references/design/web-artifacts-builder/SKILL.md](references/design/web-artifacts-builder/SKILL.md) |
| Web design guidelines review | Review UI code for Web Interface Guidelines compliance — accessibility, UX audit | [references/design/web-design-guidelines/SKILL.md](references/design/web-design-guidelines/SKILL.md) |
| Writing guidelines review | Review docs/prose for writing-style-guide compliance — tone, voice | [references/design/writing-guidelines/SKILL.md](references/design/writing-guidelines/SKILL.md) |
| Slide decks | Strategic HTML presentations with Chart.js, design tokens, responsive layouts | [references/design/slides/SKILL.md](references/design/slides/SKILL.md) |

## Anti-AI-slop frontend taste systems

Imported 2026-08-05 from two third-party repos (`taste-skill`, 12 sub-skills, MIT — Leonxlnx; `hallmark`, 1 skill, MIT — Nutlope; see [`THIRD-PARTY-LICENSES.md`](../../THIRD-PARTY-LICENSES.md)). All target the same problem as `frontend-design` above — landing pages/portfolios/UIs that read as templated AI defaults — but each encodes a different, opinionated, largely-incompatible rule set. They are **not meant to be combined on one build**: pick one taste system per project rather than layering several.

| Domain | When to use | Full guide |
|---|---|---|
| Taste-skill (anti-slop frontend, current) | Landing pages, portfolios, redesigns — brief inference into a one-line "design read," 3 numeric dials (variance/motion/density), routes to an official design system (Fluent/Material/Carbon/etc.) when the brief calls for one vs. a native-CSS aesthetic otherwise, plus hard layout/color/copy/image rules and GSAP sticky-stack/horizontal-pan code skeletons | [references/design/taste-skill/SKILL.md](references/design/taste-skill/SKILL.md) |
| Taste-skill v1 (legacy) | Same lineage as `taste-skill`, kept only for projects that depend on its exact pre-rewrite behavior — the vendor's own current default is v2 (`taste-skill`) | [references/design/taste-skill-v1/SKILL.md](references/design/taste-skill-v1/SKILL.md) |
| Hallmark | Greenfield pages, `audit`/`redesign`/`study`(DNA-extraction-from-URL-or-screenshot) verbs, component-scope mode for single UI elements — a 20-theme catalog + custom-OKLCH route, named macrostructure/nav/footer archetype picks with mandatory rotation via a `.hallmark/log.json` diversification log, and a 58-gate pre-emit slop test | [references/design/hallmark/SKILL.md](references/design/hallmark/SKILL.md) |
| Brand-kit image generation | Premium brand-guidelines boards, logo systems, identity decks, visual-world presentations — generated as images, not a text brand-voice doc (see `brand` above for that) | [references/design/brandkit/SKILL.md](references/design/brandkit/SKILL.md) |
| Industrial/brutalist UI | Swiss-typographic-print + military-terminal-HUD fusion for data-heavy dashboards, portfolios, or editorial sites that should read as "declassified blueprints" | [references/design/brutalist-skill/SKILL.md](references/design/brutalist-skill/SKILL.md) |
| GSAP motion + AIDA structure | Awwwards-tier motion engineering — a simulated Python RNG seed forces layout/font/component variance, strict AIDA (Attention/Interest/Desire/Action) page structure, gapless `grid-flow-dense` bento grids, concrete GSAP ScrollTrigger recipes | [references/design/gpt-tasteskill/SKILL.md](references/design/gpt-tasteskill/SKILL.md) |
| Image-to-code (image-first workflow) | For visually important web tasks: generate the design image(s) first, analyze them deeply, then implement code to match as closely as possible — written for Codex-style agents that under-generate images by default | [references/design/image-to-code-skill/SKILL.md](references/design/image-to-code-skill/SKILL.md) |
| Mobile app image direction | Generates premium iOS/Android/cross-platform app screen concepts as phone-mockup-framed images — image generation only, does not write code | [references/design/imagegen-frontend-mobile/SKILL.md](references/design/imagegen-frontend-mobile/SKILL.md) |
| Web image direction | Generates one horizontal reference image per landing-page section (never compressed into one board) for a human or coding model to recreate — image generation only, does not write code | [references/design/imagegen-frontend-web/SKILL.md](references/design/imagegen-frontend-web/SKILL.md) |
| Minimalist editorial UI | Warm monochrome palette, typographic contrast, flat bento grids, muted pastel accents — no gradients, no heavy shadows, no pill-shaped large containers | [references/design/minimalist-skill/SKILL.md](references/design/minimalist-skill/SKILL.md) |
| Redesign existing projects | Audit-first upgrade path for an already-built site/app: scan the current stack, list every generic-AI pattern found, fix in place without migrating frameworks or breaking functionality | [references/design/redesign-skill/SKILL.md](references/design/redesign-skill/SKILL.md) |
| High-end agency visual design | A named "$150k-agency" spec — banned fonts/icons/shadows/layouts/motion, a vibe+layout archetype picker, double-bezel nested card architecture, magnetic-button physics, plus a pre-output checklist | [references/design/soft-skill/SKILL.md](references/design/soft-skill/SKILL.md) |
| Stitch `DESIGN.md` generator | Produces a semantic `DESIGN.md` file for Google Stitch's screen generator — translates this same anti-slop rule family into Stitch's natural-language visual-description format instead of code | [references/design/stitch-skill/SKILL.md](references/design/stitch-skill/SKILL.md) |

### `taste-skill` vs `hallmark` — why not both on one build

These are the two "full pipeline" systems in the table above (the other 11 are narrower: a named aesthetic, an image-only workflow, or a non-code output format). Both take a vague brief and turn it into a complete, non-generic page — but through two incompatible mechanisms. Running both on the same build means two different logics both trying to decide the same things (what theme, what structure, what to do differently this time), and one will silently overwrite the other's decision.

| Axis | `taste-skill` | `hallmark` |
|---|---|---|
| **First move** | Infers a one-line "design read" from the brief itself (page kind, vibe words, audience) — asks the user only if genuinely ambiguous, and only one question | **Always** asks the user 3 questions (audience/use-case/tone) before writing anything, even on a fully-detailed brief — silence/"go ahead" triggers inference instead, but the ask happens first |
| **Style selection mechanism** | 3 numeric dials, `DESIGN_VARIANCE`/`MOTION_INTENSITY`/`VISUAL_DENSITY` (1–10 each), set from a signal→value lookup table | 20 *named* themes (Specimen, Atelier, Brutal, Bloom, …) in a catalog, or a custom-built OKLCH palette — no numeric dials at all |
| **Page structure** | No catalog of page shapes — structure emerges from a pile of individual hard rules (hero must fit in one viewport, max 2 consecutive image/text-split sections, bento cell-count must match content count, etc.) | Picks one of 21 *named* macrostructures (Bento Grid, Long Document, Manifesto, …) **first**, before any visual rule loads — the macrostructure decides hero/section/CTA shape as one bundled choice |
| **Existing-project awareness** | No pre-flight step — starts reasoning from the brief, doesn't read the project's current fonts/palette/spacing before proposing new ones | Step 0 always scans `package.json`/Tailwind config/CSS first and states what it found, then preserves those tokens by default |
| **Cross-build memory** | None — every invocation is judged independently, nothing is written to disk | Writes `.hallmark/log.json` after every build and **requires** the next build's macrostructure/theme/nav/footer to differ from the last 3–5 entries |
| **Named verbs** | None — one default flow only (an adjacent sibling skill, `redesign-skill`, covers the upgrade-existing-site case as a *separate file*) | `hallmark audit` (score only, no edits), `hallmark redesign` (in-place restructure), `hallmark study <url\|screenshot>` (extract another site's design DNA) |
| **Official design-system routing** | Has one: routes to Fluent UI/Material 3/Carbon/Primer/govuk-frontend/etc. when the brief matches an enterprise/public-sector pattern, native CSS otherwise | Has none — always produces its own catalog/custom look, never suggests installing a vendor design-system package |
| **Component-only mode** | Not distinguished from a full page | Has a dedicated Component-scope flow with different rules when the brief is one UI element, not a page |

**Concrete failure mode if you layer them**: say `hallmark` picks the *Lumen* theme, which pins the display font to Instrument Serif as one of its signature moves — but `taste-skill`'s own hard rule (§4.1) explicitly **bans** Instrument Serif as an LLM-favorite default. Follow both in the same build and you get a contradiction with no resolution rule, because neither skill knows the other exists. The same collision shows up structurally: `hallmark` commits to a macrostructure before any visual rule loads; `taste-skill` has no macrostructure concept, so there's nothing for `hallmark`'s pick to compose with — you'd be trying to run two different "what does this page's skeleton look like" decisions on the same output. And if you alternate between them across builds in the same project, `hallmark`'s diversification memory (`.hallmark/log.json`) never sees what `taste-skill` produced, so it can't actually enforce "different from last time" — the one mechanism that makes `hallmark` worth using for multi-page consistency silently stops working.

**Decision guide — pick one per project:**
- Want the **official-design-system routing** (the brief reads as enterprise SaaS, Shopify, a public-sector service, etc.) → `taste-skill` is the only one of the two with this capability.
- Building **many pages/many test variants in the same project** and need them to genuinely not repeat → `hallmark`'s `.hallmark/log.json` is the only mechanism here built for that; `taste-skill` has no memory across invocations.
- Need to **audit an existing site without editing it**, or **extract a look from a URL/screenshot** → both are `hallmark`-only (`audit`, `study`); `taste-skill` has neither.
- Upgrading an existing site rather than building greenfield → either `hallmark redesign` or the separate `redesign-skill` file — not `taste-skill`'s default flow, which assumes a fresh build.
- Otherwise (a single one-off landing page/portfolio, no existing project constraints, no multi-build consistency need) → either works; `taste-skill`'s brief-inference is lighter-weight (fewer mandatory questions), `hallmark`'s 58-gate slop test and named macrostructures are more prescriptive/heavier. Default to whichever the user names; if neither is named, `taste-skill` is the lower-friction default for a single build.

## Animation, motion & UI-decision tools (Emil Kowalski)

Imported 2026-08-09 from the third-party repo `emilkowalski/skills` (9 sub-skills, MIT — Emil Kowalski, of Sonner/Vaul; see [`THIRD-PARTY-LICENSES.md`](../../THIRD-PARTY-LICENSES.md)). Distinct from every table above: those are about visual language (color, layout, typography, named aesthetics); this cluster is specifically about **interface motion mechanics** (easing curves, spring physics, gesture velocity, interruptibility, performance) plus two adjacent UI-decision tools from the same source pack. `hallmark`'s own `motion.md` (in the table above) covers similar ground at a much shallower depth as one facet of its broader pipeline — read that skill's own description for the comparison; this cluster is the deep, dedicated version.

| Domain | When to use | Full guide |
|---|---|---|
| Design engineering philosophy (main entry point) | Emil Kowalski's overall taste/polish philosophy for component design and animation decisions — mostly motion, some general UI polish. Start here for open-ended "make this feel better" requests | [references/design/emil-design-eng/SKILL.md](references/design/emil-design-eng/SKILL.md) |
| Build an animation | Turn a request for motion into an implementation — should it animate at all, what tool, what curve/duration/spring, how it exits | [references/design/animate/SKILL.md](references/design/animate/SKILL.md) |
| Review animations (diff) | Strict review of animation/motion code in a diff against a 10-point non-negotiable standard; block/approve verdict | [references/design/review-animations/SKILL.md](references/design/review-animations/SKILL.md) |
| Audit & plan animation fixes (codebase) | Survey a whole codebase's motion, produce a prioritized audit and self-contained fix plans for any executor to run | [references/design/improve-animations/SKILL.md](references/design/improve-animations/SKILL.md) |
| Find animation opportunities | Read-only sweep of a UI for places that would genuinely benefit from motion — and what to explicitly reject | [references/design/find-animation-opportunities/SKILL.md](references/design/find-animation-opportunities/SKILL.md) |
| Animation vocabulary (naming) | Reverse-lookup glossary: turn a vague description ("the bouncy thing when a popover opens") into the precise term | [references/design/animation-vocabulary/SKILL.md](references/design/animation-vocabulary/SKILL.md) |
| Apple-style fluid interfaces | Apple's WWDC-derived spring physics, gesture interruptibility, velocity handoff/projection, materials/depth, typography optical sizing | [references/design/apple-design/SKILL.md](references/design/apple-design/SKILL.md) |
| Pick a UI library | Curated, opinionated library recommendation for a frontend task (toasts, dropdowns, charts, drag-and-drop, state, styling…) instead of hand-rolling or guessing | [references/design/pick-ui-library/SKILL.md](references/design/pick-ui-library/SKILL.md) |
| Multi-variant UI prototyping | Build several genuinely different versions of one described UI piece behind a live visual picker, then promote the winner. Renamed from the source repo's `prototype` to avoid colliding with `agentic-dev-workflow/prototype` (a single-artifact throwaway-validation skill — different mechanism, see that skill's own description for the comparison) | [references/design/prototype-variants/SKILL.md](references/design/prototype-variants/SKILL.md) |

**Picking within this cluster**: `animate` builds, `review-animations` reviews a diff, `improve-animations` audits/plans across a whole codebase (read-only, hands off execution), `find-animation-opportunities` only searches for *missing* motion (also read-only) — the four compose in a pipeline (`find-animation-opportunities` → `improve-animations plan` → any executor → `review-animations`) but each is invoked for a different verb. `animation-vocabulary` is pure naming, not building or judging. `apple-design` is a deeper physics/gesture layer that `animate`/`review-animations` reference for spring configs but don't duplicate — reach for it directly when the ask is gesture-driven (drag, swipe, sheet) rather than a fixed-duration transition. `pick-ui-library` and `prototype-variants` are UI-decision tools bundled with the same pack, not visual-design generators.

## How to use this skill

1. **Producing a new deliverable** (banner, slide deck, logo, page-at-scale): start from the closest-matching domain's template/data rather than designing from a blank page — that consistency is the entire point of this category.
2. **Reviewing existing output**: `web-design-guidelines` and `writing-guidelines` are compliance-review domains, not generation domains — use them to audit, not to create.
3. **When multiple design domains could apply** (e.g. a slide deck needs both `slides` and `theme-factory` and `design-system` tokens), it's normal to pull from more than one — they're meant to compose.
4. Most design domains bundle real reference data (CSVs of palettes/fonts/charts, preset theme definitions, component templates) one level under their own directory — read those on demand rather than trying to hold the full database in context; that's why they're structured as references rather than inlined here.
5. **`brand` vs `anthropic-brand-guidelines`**: these sound similar but solve different jobs. `brand` helps *define or apply a brand system you're building* (any company's). `anthropic-brand-guidelines` applies one specific, already-fixed brand (Anthropic's) — reach for it only when the artifact is explicitly meant to carry Anthropic's look-and-feel, not as a generic "make this branded" request.
6. **`design` vs `brand`/`design-system`/`banner-design`/`slides`**: these all come from the same source package and deliberately overlap — `design` is the all-in-one entry point that bundles its own Logo/CIP/Banner/Icon/Social-Photo/Slides generation inline (one trigger, everything), while `brand`, `design-system`, `banner-design`, and `slides` are the same package's standalone, narrower versions of those individual pieces. Neither is more "correct" — pick `design` for a request spanning multiple deliverables or when unsure, pick the standalone skill when the request is for exactly one of those things and a smaller context load is preferable. Do not merge or treat one as deprecated; the duplication is intentional on the source package's part.
7. **Picking one taste system from the "Anti-AI-slop frontend taste systems" table**: these 13 sub-domains were read in full against each other and against `frontend-design`/`brand` — none turned out to be a true duplicate (each has its own concrete, largely incompatible rule set: different banned-font lists, different dial systems, different code skeletons), so all were kept as separate files with cross-references rather than merged. Pick by scope, not by reading every one:
   - Building a **full landing page/portfolio/redesign with actual code** → `taste-skill` (current) or `hallmark` — pick one, don't run both on the same build; see the "`taste-skill` vs `hallmark`" comparison above the table for which fits.
   - Want a **specific named aesthetic** rather than the general anti-slop pipeline → `brutalist-skill`, `minimalist-skill`, `soft-skill`, or `gpt-tasteskill` (motion-heavy Awwwards spec).
   - Already have an existing site to fix rather than a greenfield build → `redesign-skill` (or `hallmark redesign`).
   - Need **images, not code** → `imagegen-frontend-web` (one image per section), `imagegen-frontend-mobile` (app screens in device mockups), or `brandkit` (brand-guidelines boards/logo decks).
   - Working in **Codex** specifically and want it to generate reference images before writing code → `image-to-code-skill`.
   - Working in **Google Stitch** rather than a code-generating agent → `stitch-skill` (emits `DESIGN.md`, not code).
   - Need **exact backward compatibility** with an existing project already pinned to the original taste-skill behavior → `taste-skill-v1`.
8. **Not a design skill despite arriving in the same import**: the `taste-skill` source repo also bundled `output-skill` (anti-truncation / complete-output enforcement) — that's unrelated to visual design, so it was folded into `product-verification/references/output-skill` instead. Don't re-add it here.
9. **Animation/motion requests** ("build this animation", "review this transition", "audit our motion", "what's this effect called", "make this feel more alive"): go to the "Animation, motion & UI-decision tools" table above, not the taste-system table — these are mechanics (curves, springs, performance, gesture physics), not visual language. Pick by verb: build → `animate`, review a diff → `review-animations`, audit a whole codebase → `improve-animations`, find missing motion → `find-animation-opportunities`, name an effect → `animation-vocabulary`, gesture/spring physics specifically → `apple-design`, general polish philosophy or unsure where to start → `emil-design-eng`.

## Note on scope

Marketing content-strategy skills (copywriting, ads, SEO audits, etc.) are deliberately **not** folded into this category even though some produce templated output — they're domain playbooks about *what to say*, not scaffolding for *how a deliverable is structured*, and remain as independent skills.
